{
  "name": "terminal",
  "version": "0.1.0",
  "private": true,
  "description": "Economstock Terminal — dashboard saham IDX dengan data realtime dari Google Sheets, deploy di Vercel.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'static site, no build step'",
    "start": "vercel dev"
  }
}
