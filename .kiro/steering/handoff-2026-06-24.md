# Handoff — Sesi 2026-06-24

Ringkasan kerja & keputusan sesi ini supaya sesi baru bisa lanjut tanpa kehilangan konteks.

## PR yang dibuat sesi ini
| PR | Isi | Status |
|---|---|---|
| #271 | Valuasi: basis TTM jadi list per-kuartal (Q1..Q4) + market cap live | merged |
| #272 | FCF disetahunkan (DCF/FCF) + % perubahan kolom Thn Berjalan/TTM | merged |
| #273 | Admin: bisa SET/mengurangi masa aktif (mis. 1 hari) | open |
| #274 | Konsensus: target price dikunci ke kolom F | merged |
| #275 | Admin: Pengaturan Billing collapsed default | open |
| #276 | Billing: aktivasi HANYA saat lunas (fix webhook Mayar) + revenue riil | open |
| #277 | Admin permanen (tanpa masa aktif) + mobile firm donut + performa firm (stop-target/SELL/NEUTRAL) + billing 2 kolom (Tools/Telegram) | open |
| #278 | Login: link "beranda" → economstock.com | open |
| #279 | Headlines: +5 sumber (Katadata, EmitenTrust, IDX Channel, IDN Financials, Kabar Bursa) | open |
| #280 | **Valuasi: kunci rumus basis TAHUNAN sesuai Excel pemilik** | open |

## Rumus valuasi (TERKUNCI) — lihat `ringkasan-rumus-basis-tahunan.md`
Basis TAHUNAN sudah final & divalidasi 100% dgn Excel ANTM:
- Avg PBV/PER/PSR = mean N tahun (window 3/5/7/10) dari kolom I–Z; H dikecualikan.
- DPR = tahun terbaru (tidak dirata-rata).
- EPS = kolom sumber (ANTM 299,98) untuk PER **dan** dividen (opsi A).
- Ramp target = `Last × (1 + (Combine + DivYield) × n)`.
- `overrides.json` dikosongkan (semua saham seragam).

### WAJIB setelah PR #280 merge
Jalankan workflow **`refresh-valuation`** (Actions → Run workflow) untuk regenerasi
957 JSON. Tanpa ini, frontend masih baca input lama. Lalu hard-refresh.

## Yang BELUM dikerjakan / catatan
- **Basis TTM/kuartal** (Q1/Q2..) di Ringkasan BELUM disamakan ke rumus terkunci
  (masih logika lama dari PR #271). Kalau mau, samakan terpisah setelah #280 stabil.
- Banyak PR (#273,275,276,277,278,279) masih OPEN — perlu di-merge + sebagian perlu
  aksi: #276 redeploy Worker utama `src/` (auto via Git push ke main) + bersihkan user
  aktif-keliru di admin.

## Catatan deploy (penting)
- **Worker utama `src/`** (auth/billing/admin/`/api/*`) + situs: auto-deploy via
  Cloudflare yang tersambung Git → merge ke main = deploy otomatis. Cek tab
  Deployments project `terminal`.
- **Worker `worker/` (`terminal-live`)**: serve `/live.json` & `/consensus.json`.
  Deploy MANUAL (`cd worker && npx wrangler deploy`) ATAU belum ada CI auto-deploy.
  Perubahan parsing konsensus (PR #274) butuh redeploy Worker ini.
- **Harga live di UI** digerakkan feed Worker `/live.json` (poll 3 mnt, gated jam
  bursa + token langganan), BUKAN workflow refresh-data 5 menit ([skip ci]).
- `data.json` (konsensus/history) di-rebuild via workflow `refresh-data`
  (mode consensus/full) → memicu redeploy Pages.
- `rec-status.json` (RETURNS/RISK rekomendasi terbaru) via workflow `refresh-ohlc`.

## Preferensi user
- Komunikasi Bahasa Indonesia.
- Peduli kecepatan; tidak suka iterasi/validasi terlalu lama.
- Area Consensus/Dashboard/Simulasi + build pipeline = "terkunci" (lihat
  `protected-areas.md`) — konfirmasi sebelum ubah logika.
