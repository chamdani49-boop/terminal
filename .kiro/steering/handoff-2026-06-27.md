---
inclusion: manual
---

# Handoff Sesi — 2026-06-27

Lanjutan dari `handoff-2026-06-23.md`. Repo: **chamdani49-boop/terminal** (Cloudflare Worker `src/` + static assets `public/`; D1 database `terminal-db`).

## Status
- Branch kerja: `main`. Semua fitur sesi ini sudah di-PR & sebagian besar di-merge oleh owner.
- Komunikasi & catatan: **Bahasa Indonesia**. Owner sangat peduli kecepatan — eksekusi langsung, push ke branch baru (jangan ke main), pakai github power tools.

## Yang dikerjakan sesi ini (PR #288–#303)
- **#288 Referral "Ajak Teman"**: +3 hari/user baru (stacking, unlimited), reward saat user baru daftar, anti-curang B+ (1 reward/referee atomik, email terverifikasi, IP referee≠referrer). Tombol "Ajak Teman" di billing (atas paket). Statistik N orang·M hari di pop-up akun. Butuh **migrasi `0007_referral.sql`**.
- **#289/#291 UI billing harga**: harga normal (coret) BESAR di atas, harga real ("bintang"/accent) di bawah.
- **#290 Admin tabel user**: paginasi (slide 100/hal), kolom "Terakhir Aktif" (dari MAX account_ips.seen_at), filter aktivitas, tombol **Download Excel/CSV** (email+username, ikut filter). Hapus user = hanya baris user (data turunan dibiarkan, sesuai permintaan owner).
- **#292/#293 Panduan Terminal**: tur interaktif (spotlight+kartu, tanpa library) di `public/panduan.js`. Auto-start sekali, replay via tombol billing, lintas-halaman ke billing. Lengkap tiap kolom + semua popup. Opsi HP/PC. Tombol billing non-aktif = nudge.
- **#299 Privasi & Panduan "sekali per email"**: hapus re-prompt consent 30 hari (`legal.js` `hasAcceptedCurrent` tak cek kedaluwarsa, cukup per TOS_VERSION). Panduan ditandai server per-akun: kolom `users.guide_seen` + `POST /api/guide-seen` + `/api/me` kirim `guide_seen`. Butuh **migrasi `0008_guide_seen.sql`**.
- **#297–#303 Perbaikan kolom cari saham di HP** (Dashboard/Simulasi/Pasar; Valuasi pakai mekanisme asli). **AKAR MASALAH FINAL: keyboard Android (GBoard) mengirim `key='Unidentified'`/`keyCode=229` (IME) untuk semua tombol**, jadi keydown/keyup tak bisa deteksi Enter. **SOLUSI (#303, berhasil):** deteksi Enter via event **`beforeinput` `inputType==='insertLineBreak'`** + atribut **`enterkeyhint="search"`** pada input. Handler delegasi di `document` fase capture (keydown+keyup+beforeinput), dedupe 300ms, di `buildSearchIndex()`.

## ⚠️ WAJIB dicek/dijalankan ke D1 (kalau belum)
```
wrangler d1 execute terminal-db --remote --file=migrations/0007_referral.sql
wrangler d1 execute terminal-db --remote --file=migrations/0008_guide_seen.sql
```
Semua kode fail-safe: sebelum migrasi jalan, fitur terkait diam-diam non-aktif tapi login/billing tetap normal.

## Konvensi penting (jangan dilanggar)
- **JANGAN sertakan file auto-refresh ke PR fitur**: `public/data.json`, `headlines.json`, `insights.json`, `macro.json`, `ohlc.json`, `valuation/*.json` (ditulis ulang GitHub Actions tiap ~15 mnt → konflik). PR fitur = file kode saja.
- Selalu **github power tools** (push_to_remote/pull_repository/create_pull_request), push ke **branch baru**, jangan ke main.
- **Sinkron main sering basi** — selalu `pull_repository main` dulu sebelum branch baru, dan verifikasi commit terbaru ada (beberapa kali ketemu local main belum tertarik PR yang sudah di-merge).
- Validasi inline JS HTML pakai `vm.Script` per blok `<script>` (node --check tak bisa untuk HTML). Set `unset NODE_OPTIONS` dulu (ada preload proxy yang mengganggu node).
- Perbaikan khusus HP: scope ketat ke `@media(max-width:560px)` / cek `!isDesktopView()`; jangan ubah perilaku PC.

## Catatan teknis berguna
- Pencarian saham: input `#searchInput`/`#searchInputMobile` (dashboard, fn `selectStock`), `#savingStockSearch` (simulasi, `savingPickStock`→set `#savingStock` dipakai `calculateSaving`), `#plSelA/#plSelB/#plSeasSelect` (pasar, `plSearchDispatch`), `#vlStockSearch` (valuasi, `window.vlSelectStock`, punya handler keydown asli sendiri — JANGAN diintersep handler delegasi/stopPropagation, bikin dropdown tak menutup).
- Popup detail rekomendasi (dashboard "Rekomendasi Analis"): `#recDetailModal` (showRecDetail/closeRecDetail). Di HP `.rec-modal` perlu `min-width:0` + font dikecilkan agar angka 5-digit tak overflow (flex-item min-width:auto bikin modal melebihi layar).
- Mesin valuasi: `scripts/build-valuation.py`, basis TTM riil + annual 2025, lihat `.kiro/steering/ringkasan-rumus-basis-tahunan.md`.

## Yang mungkin lanjut berikutnya
- Verifikasi #303 benar-benar fix Enter di semua menu HP (owner konfirmasi "berhasil").
- Idempotensi webhook Mayar (ditandai "nanti" sejak handoff lama).
- Isi konten tombol "Panduan Terminal" bila perlu materi tambahan.
